<?xml version="1.0" encoding="UTF-8"?>
<!-- 从高到地低 OFF 、 FATAL 、 ERROR 、 WARN 、 INFO 、 DEBUG 、 TRACE 、 ALL -->
<!-- 日志输出规则  根据当前ROOT 级别，日志输出时，级别高于root默认的级别时  会输出 -->
<!-- 以下  每个配置的 filter 是过滤掉输出文件里面，会出现高级别文件，依然出现低级别的日志信息，通过filter 过滤只记录本级别的日志-->


<!-- 属性描述 scan：性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true scanPeriod:设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。
    debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration>
    <property name="LOG_CHARSET" value="UTF-8"/>
    <property name="BASE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level- [%thread] %logger{5} - %msg%n"/>
    <property name="SYSTEM_LOG_PATTERN"
              value=" %d{yyyy-MM-dd HH:mm:ss.SSS} |%-5level|[%thread]%logger{5}|%X{oprSeq}|%X{terminalVersion}|%X{phoneModel}|%X{oprCode}|%X{MSG}|%msg%n"/>
    <appender name="console_base" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${BASE_PATTERN}</pattern>
            <charset>${LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <appender name="console_system" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${SYSTEM_LOG_PATTERN}</pattern>
            <charset>${LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <appender name="fileInfoLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>
                [%d{yyyy-MM-dd HH:mm:ss.SSS}] [%5level] [%thread] [%t] [%t] %logger{0} %msg%n
            </pattern>
        </encoder>
        <!--滚动策略-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--路径-->
            <fileNamePattern>E:/loggerTest1/business.%d.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <root level="info">
        <appender-ref ref="console_base"/>
        <appender-ref ref="fileInfoLog"/>
    </root>
    <logger name="com.pc.business" level="DEBUG" additivity="false">
        <appender-ref ref="console_system"/>
    </logger>
</configuration>
