<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pc.gateway.mapper.LoginMapper">
    <select id="getUserCount" parameterType="java.lang.String" resultType="com.pc.gateway.bean.UserInfoBean">
        SELECT
          user_id userId,
          user_name userName
        FROM r_sys_user
        WHERE user_name = #{userName}
        AND password = #{password}
    </select>

    <select id="getUserInfo" parameterType="java.lang.String" resultType="com.pc.gateway.bean.UserInfoBean">
        SELECT
          user_id userId,
          user_name userName
        FROM r_sys_user
        WHERE user_id = #{userId}
    </select>
    <select id="loginInfo" parameterType="java.lang.String" resultType="com.pc.gateway.bean.UserInfoBean">
        SELECT
          user_id userId,
          user_name userName
        FROM r_sys_user
        WHERE user_id = #{userId}
    </select>
</mapper>