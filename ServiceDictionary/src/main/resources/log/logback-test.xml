<?xml version="1.0" encoding="UTF-8"?>
<!-- 日志级别：TRACE, DEBUG, INFO, WARN, ERROR, ALL 和 OFF -->
<!-- 日志输出规则根据当前ROOT级别，日志输出时，级别高于root默认的级别时会输出 -->
<!-- 以下每个配置的filter是过滤掉输出文件里面，会出现高级别文件，依然出现低级别的日志信息，
     通过filter过滤只记录本级别的日志 -->
<!-- 属性描述 scan：性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true scanPeriod:设置监测
     配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的
     时间间隔为1分钟。
    debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration>
    <property name="LOG_CHARSET" value="UTF-8"/>
    <property name="FILE_MAX_HISTORY" value="30}"/>
    <property name="LOG_HOME" value="./logs/dictionary}"/>
    <property name="NAME" value="dictionary"/>
    <property name="BASE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level- [%thread] %logger{5} - %msg%n"/>
    <property name="SYSTEM_LOG_PATTERN"
              value=" %d{yyyy-MM-dd HH:mm:ss.SSS} |%-5level|[%thread]%logger{5}|%X{oprSeq}|%X{terminalVersion}|%X{phoneModel}|%X{oprCode}|%X{MSG}|%msg%n"/>
    <!--日志基础配置~控制台打印-->
    <appender name="CONSOLE_BASE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${BASE_PATTERN}</pattern>
            <charset>${LOG_CHARSET}</charset>
        </encoder>
    </appender>
    <!--日志中级配置配置~文件打印-->
    <appender name="FILE_SYSTEM"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>
            ${LOG_HOME}/${NAME}.log
        </file>
        <rollingPolicy
                class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>
                ${LOG_HOME}/${NAME}-%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <MaxHistory>
                ${FILE_MAX_HISTORY}
            </MaxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${SYSTEM_LOG_PATTERN}</pattern>
            <charset>${LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <!--<appender-ref ref="CONSOLE_BASE"/>-->
        <appender-ref ref="FILE_SYSTEM"/>
    </root>
    <logger name="com.pc.dictionary" level="DEBUG" additivity="false">
        <appender-ref ref="FILE_SYSTEM"/>
    </logger>
</configuration>